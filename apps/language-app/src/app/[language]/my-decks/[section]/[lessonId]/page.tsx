'use client'

// This route loads FlashCardsV2 in custom-deck mode.
// The lessonId will be 'custom-deck' and section is the deck key prefix.
export { default } from '@/app/[language]/flashcards/[section]/[lessonId]/page';
