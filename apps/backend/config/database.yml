default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: wallerverse_development

test:
  <<: *default
  database: wallerverse_test

production:
  primary:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
  cache:
    <<: *default
    database: wallerverse_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    database: wallerverse_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    database: wallerverse_production_cable
    migrations_paths: db/cable_migrate
